1) java -jar "C:\Users\victus-pc\Downloads\INSManager-8.0\INSManager-8.0.jar"
-------------------------------------------------------------------------------------------------------------------------------------------------
2) ./anpp_log_decoder "/home/entropy/Underwater_Software/IMU/C/Example Logs/SpatialLog_13-06-21_13-46-42.anpp"
-------------------------------------------------------------------------------------------------------------------------------------------------
3) in POWERSHELLL administrator => Check the name before: usbipd list
You only need to run 'usbipd bind --busid [name]' once.
Windows will remember this forever (or until you unplug the device and plug it into a different USB port).
Even if you restart your computer, the "Bind" stays active.

4) You must run 'usbipd attach --wsl --busid [name]' every time you plug the sensor in or restart your computer.

5) in vscode terminal => sudo ./packet_example ttyUSB0 115200
-------------------------------------------------------------------------------------------------------------------------------------------------
6) first get inside ardupilot-master folder inside IMU => click on Docker Desktop icon then in the tray and wait till the docker icon appears in the tray icons
 => docker run --rm -it -v "$(pwd):/ardupilot" -u "$(id -u):$(id -g)" ardupilot:latest bash

7) 
=> Configure (The Map): ./waf configure --board navigator --no-submodule-update
(Only needs to be run once, or if you add new files to a wscript) NOT after each restart.

=> Compile (The Action): ./waf sub
(Run this every time you change your "Link Active" message or add logic).
-------------------------------------------------------------------------------------------------------------------------------------------------
8) cd ~/Underwater_Software/IMU/ardupilot-master
git add libraries/AP_ExternalAHRS/AP_ExternalAHRS_Orientus.*
git commit -m "Orientus: Implemented packet ID 20 decoding"
git push origin master

cd ~/Underwater_Software/IMU
git add ardupilot-master
git commit -m "Submodule: Point to latest Orientus driver commit"
git push origin main
-------------------------------------------------------------------------------------------------------------------------------------------------
9) ssh pi@192.168.2.2 => password: raspberry => sudo su => cd /root/.config/blueos/ardupilot-manager/firmware/
then: reading the file => cat elie_debug.txt
      live monitoring => tail -f elie_debug.txt

debug: Sub::write_debug("Hello World!");
-------------------------------------------------------------------------------------------------------------------------------------------------
10) export project structure (cd inside where you want to have the structure): tree -I 'node_modules|.git' -L 3 > structure.txt
11) export to ai-friendly format: npx repomix --ignore "**/.git/**,**/node_modules/**,**/dist/**,**/build/**,**/*.log,**/*.bin,**/*.tmp,**/.env"